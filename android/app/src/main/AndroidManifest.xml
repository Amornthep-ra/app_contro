<!-- android/app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- อุปกรณ์ต้องมี Bluetooth / BLE (ตั้งเป็น true ถ้าอยากจำกัดเฉพาะเครื่องที่มี BT เท่านั้น) -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- ===== Permissions สำหรับ Android ≤ 11 (API 30) ===== -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- สแกน BLE บน Android ≤ 11 ต้องใช้ Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="30" />

    <!-- ===== Permissions สำหรับ Android 12+ (API 31+) ===== -->
    <!-- SCAN: สำหรับค้นหา/สแกน (ไม่ใช้ตำแหน่ง) -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <!-- CONNECT: สำหรับเชื่อมต่อ/สื่อสาร (ทั้ง BLE และ Classic SPP) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- ถ้าต้องการโฆษณา BLE (Peripheral) ให้เปิดบรรทัดนี้ -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" /> -->

    <!-- (ตัวเลือก) ถ้าแอปต้องใช้อินเทอร์เน็ตให้เปิด -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->

    <application
        android:name="${applicationName}"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- (ตัวเลือก) ถ้าต้อง query อินเทนต์อื่น ๆ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
